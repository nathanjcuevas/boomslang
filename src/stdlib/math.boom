def gcd(int a, int b) returns int:
	loop while a != b:
		if a > b:
			a = a - b
		else:
			b = b - a
	return a

def pow(int a, int b) returns int:
	if b == 0:
		return 1
	if b == 1:
		return a
	int i = 0
	int ans = 1
	loop i+=1 while i<b:
		ans *= a
	return ans

